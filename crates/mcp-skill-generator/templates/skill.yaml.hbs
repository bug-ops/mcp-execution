---
name: {{name}}
description: {{description}}
---

# {{name}} MCP Skill

This skill provides access to {{server_id}} tools via Model Context Protocol (MCP).

## Overview

Server **{{server_id}}** exposes {{tool_count}} tools for {{description}}.

## Available Tools

{{#each tools}}
### `{{this.name}}`

{{this.description}}

{{#if this.parameters}}
**Parameters:**
{{#each this.parameters}}
- `{{this.name}}` ({{this.type_name}}{{#if this.required}}, required{{/if}}): {{this.description}}
{{/each}}
{{/if}}

{{/each}}

## Usage

To use this skill, Claude will automatically invoke the appropriate tools based on your requests. The tools communicate with {{server_id}} via MCP protocol.

---

*Generated by mcp-skill-generator {{generator_version}} on {{generated_at}}*
