{
  "name": "{{server_name}}",
  "version": "{{server_version}}",
  "description": "Generated MCP tool definitions for {{server_name}}",
  "tools": [
{{#each tools}}
    {
      "name": "{{name}}",
      "typescript_name": "{{typescript_name}}",
      "description": "{{description}}"
    }{{#unless @last}},{{/unless}}
{{/each}}
  ],
  "generated_at": "{{timestamp}}",
  "generator": "mcp-codegen"
}
