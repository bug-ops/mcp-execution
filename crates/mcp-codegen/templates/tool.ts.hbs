/**
 * {{description}}
 *
 * @param params - Tool parameters
 * @returns Tool execution result
 */
export async function {{typescript_name}}(params: {{typescript_name}}Params): Promise<{{typescript_name}}Result> {
  // Call the MCP tool via bridge
  const result = await callTool('{{name}}', params);
  return result as {{typescript_name}}Result;
}

/**
 * Parameters for {{typescript_name}} tool.
 */
export interface {{typescript_name}}Params {
{{#each properties}}
  {{#if required}}
  {{name}}: {{type}};
  {{else}}
  {{name}}?: {{type}};
  {{/if}}
{{/each}}
}

/**
 * Result type for {{typescript_name}} tool.
 */
export interface {{typescript_name}}Result {
  // Tool-specific result structure
  [key: string]: unknown;
}
