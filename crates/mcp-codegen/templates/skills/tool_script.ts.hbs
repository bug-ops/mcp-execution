/**
 * {{description}}
 *
 * Generated by mcp-codegen. Do not edit manually.
 *
{{#each parameters}}
 * @param {{name}} - {{description}}{{#unless required}} (optional){{/unless}}
{{/each}}
 * @returns Promise<CallToolResult>
 */

import type { MCPClient, CallToolResult } from "@anthropic/mcp-types";

/**
 * Parameters for {{tool_name}}
 */
export interface {{function_name}}Params {
{{#each parameters}}
  /** {{description}} */
  {{name}}{{#unless required}}?{{/unless}}: {{ts_type}};
{{/each}}
}

/**
 * {{description}}
 *
 * @example
 * ```typescript
 * const result = await {{function_name}}(client, {
{{#each required_parameters}}
 *   {{name}}: {{example}},
{{/each}}
 * });
 * ```
 */
export async function {{function_name}}(
  client: MCPClient,
  params: {{function_name}}Params,
): Promise<CallToolResult> {
  return client.callTool("{{tool_name}}", params);
}

// Default export for convenience
export default {{function_name}};
